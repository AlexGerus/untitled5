# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build untitled5`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t untitled5`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system untitled5 && \
          adduser --system -G untitled5 untitled5

COPY  . .
RUN chown -R untitled5:untitled5 .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix untitled5 --omit=dev -f install

CMD [ "node", "untitled5" ]
